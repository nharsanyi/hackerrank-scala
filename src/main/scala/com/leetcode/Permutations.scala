package com.leetcode

import scala.collection.mutable.ListBuffer

object Permutations {

//  def permute(nums: Array[Int]): List[List[Int]] = {
//    val result = new ListBuffer[List[Int]]
//    if (nums.length == 1) {
//      result.addOne(nums.toList)
//    } else {
//      for (i <- nums) {
//        nums match {
//          case Array(h, tail@_*) => permute(tail)
//        }
//      }
//    }
//    result.toList
//  }
}
